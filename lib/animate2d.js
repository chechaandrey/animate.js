/*
 * args: app, backend, frontend, worker
 */
function Animate2D(options) {
	
	options = options || {};
	
	function __construct(opts) {
		
		// инициализация forntend'a
		// инициализация worker'a {инициализация backend'a, исполнение app} или backend'a {исполнение app}
		
	}
	
	// worker api
	
	
	
	
	
	
	
	__construct.call(this, options);
	
}
